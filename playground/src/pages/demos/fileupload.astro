---
/**
 * Demo source for docs/component-fileupload.html.
 *
 * Preview only. The States grid pads each dropzone inline and includes an
 * `is-dragover` cell — a class the browser adds during a drag, never something
 * the component renders — so that row stays hand-written.
 */
import { FileUpload } from '@iron-software/astro-components';
---

<html lang="en">
  <body>
    <div data-demo="preview">
      <FileUpload name="attachment" />
    </div>
  </body>
</html>
